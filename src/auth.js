/**
 * auth.js
 * 
 * Contains the stub code functions of all auth* functions.
 */

function authLoginV1(email, password) {
  return {
    authUserId: 1
  };
}

function authRegisterV1(email, password, nameFirst, nameLast) {
  return {
    authUserId: 1
  };
}
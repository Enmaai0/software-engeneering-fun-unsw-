/**
 * admin.js
 *
 * Contains the functions of all admin* functions.
 */

function adminUserRemove(uId: number): Record<never, never> {

  return {};
}

function adminUserPermissionChange(uId: number, permissionId: number): Record<never, never> {

  return {};
}

export { adminUserRemove, adminUserPermissionChange };
